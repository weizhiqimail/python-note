# 1. [Python 的变量](./code/26-var.py)
+ Python 的变量实质上是一个指针，int, str
+ is 是比较两个对象是否是同一个对象，判断两个对象的 id 是否相等

# 2. [is 和 == 的区别](./code/27-is_==.py)
+ Python 内部会把小整数，小字符串做优化
+ `==` 会调用 list 的 `__eq__` 魔法函数
+ 判断实例用 is，不要用 ==

# 3. [del和垃圾回收](./code/28-del_gc.py)
+ Python 中的垃圾回收是靠引用计数
+ 魔法函数 `__del__` 当 CPython 垃圾回收的时候，可以释放资源

# 4. [参数错误](./code/29-error.py)
