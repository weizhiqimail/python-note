# 1. Python 中的序列分类
+ 容器序列: list, tuple, deque
+ 扁平序列: str, bytes, bytearray, array.array
+ 可变序列: list, deque, bytearray, array
+ 不可变: str, tuple, bytes

# 2. 序列类型的 abc 继承关系

# 3. [序列的 +，+= 和 extend 的区别](../code/17-list.py)
+ `+` 可以拼接两个序列，只能拼接 list，不能拼接 tuple
+ `+=` 可以拼接任意序列类型

# 4. [切片](../code/18-slice.py)
+ `l[start:end:step]`
+ 切片操作返回的是一个新列表，不会修改原有的列表

# 5. [bisect维护已排序序列](../code/19-bisect.py)
+ 用来处理已排列的序列，升序
+ 用二分查找

# 6. [array](../code/20-array.py)
+ array 只能存放指定的数据类型
+ list 可以存放任意数据类型

# 7. [列表生成式](../code/21-list_gen.py)
+ 列表生成式性能高于列表操作
+ 列表生成式简洁
+ 列表生成式的中括号变成小括号之后，列表不会变成 tuple，而是变成一个生成器
+ 字典推导式

